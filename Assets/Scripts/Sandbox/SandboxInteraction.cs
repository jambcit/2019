using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SandboxCharacterInteract
{
    // TODO script for SandboxObjectInteract
    /**
    SandboxObjectInteract interactableTarget;

    // Start is called before the first frame update
    void Start()
    {
        character = transform.parent;

        //if(!character.tag.equals("Player"))
        //{
        //  Debug.LogError("SandboxInteract.Start(): Wrong place to attached, from " + gameObject.name);
    }

    // Update is called once per frame
    void Update()
    {
        if(interactableTarget != null
        && Input.GetKeyDown(KeyCode.E))
        {
            interactableTarget.startInteract();
        }
    }

    void OnColliderEnter(Collision collision)
    {
        if(collision.gameObject.tag.equals(Tags.Interactable))
        {
            interactableTarget = collision.gameObject.GetComponent<SandboxObjectInteract>();
        }
    }
    **/
}